[Unit]
Description=Operations Command Center Frontend
After=network.target operations-backend.service

[Service]
User=root
WorkingDirectory=/opt/operations-command-center/current/frontend
EnvironmentFile=/etc/hiddenoasis/operations.env
ExecStart=/usr/bin/npm run start -- --hostname 127.0.0.1 --port 3200
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
